#include "main.h"

/**
 * _islower - a function that checks if a character is lowercase
 *@c: character
 * Return: returns 1 if TRUE or 0 if NOT
 */

int _islower(int c)
{
	
	
	if (c = 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
