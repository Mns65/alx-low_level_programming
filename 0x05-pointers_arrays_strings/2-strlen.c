#include "main.h"

/**
 * _strlen - A function that counts a string length
 *@s: The string entered
 *
 * Return: The length count
 */

int _strlen(char *s)
{
	int i;

	for (i = s[0]; i < s[n] - 1; i++)

		return (i);
}
